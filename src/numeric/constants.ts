export function piNumber(): number {
  return Math.PI;
}

export function eNumber(): number {
  return Math.E;
}

export function phiNumber(): number {
  return (1 + Math.sqrt(5)) / 2;
}

export function sqrt2Number(): number {
  return Math.sqrt(2);
}
